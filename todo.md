### Todo

1. There is an issue present in this edge server when i want to run the csv adapter it is not running 
2. Check how the csv adapter works in the datonis_edge codebase and try to fix the issue present in this edge server 
3. In the csv adapter if there is only one thing configured then i want to see only one thing on the activity panel not the previous things 
4. There are some ui issues present where toggles are present fix them
5. Also want to add that OPCUA and csv adapters are independent to each other so if i want to run only opcua it will run independently without throughing any error to configure csv adapter and vice-versa 
6. If you see any other problems fix them 
7. Maintain a .md file in which you saved what changes you done 
8. Also add this in Ui (panel where dashboard , adapter , snapshot are present) add 3 lines button so that it can be hide 
9. Use good icons and there should be uniformity in it 